{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "lerna run dev --scope \"@zini/frontend\"",
    "build": "lerna run build --scope \"@zini/frontend\"",
    "start": "lerna run start --scope \"@zini/frontend\"",
    "prettier": "lerna run prettier --scope \"@zini/frontend\"",
    "lint": "lerna run lint --scope \"@zini/frontend\"",
    "lint-staged": "lerna run lint-staged --scope \"@zini/frontend\"",
    "copy-env": "lerna run copy-env --scope \"@zini/frontend\""
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
